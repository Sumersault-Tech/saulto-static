<section class="reveal" data-controller="reveal" style="min-height: 80dvh; display: flex; align-items: center; justify-content: center; background: var(--hv2-dark); padding: 80px 24px; text-align: center;">
  <div style="max-width: 680px;">
    <p style="font-family: var(--hv2-serif); font-size: clamp(32px, 5vw, 62px); font-weight: 400; color: #fff; line-height: 1.2; margin: 0 0 32px;">
      You have 15 dashboards.
    </p>
    <p style="font-family: var(--hv2-sans); font-size: clamp(16px, 2.2vw, 22px); color: rgba(255,255,255,0.5); line-height: 1.5; margin: 0 0 28px;">
      And still can't answer one question:
    </p>
    <p style="font-family: var(--hv2-serif); font-size: clamp(28px, 4vw, 52px); font-weight: 500; color: var(--hv2-gold); line-height: 1.15; margin: 0;">
      Are we actually going to hit the number?
    </p>
  </div>
</section>
