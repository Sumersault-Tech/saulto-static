<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
  </head>
  <body class="video <%= data.page_class %>" style="margin: 0;">
    <%= yield %>
  </body>
</html>
